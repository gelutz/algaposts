meta {
  name: Create post
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/posts
  body: json
  auth: inherit
}

body:json {
  {
    "title": "Textículos",
    "body": "Piada com o fato de que a palavra que significa textos bem pequenos (textículos) parece testículos.",
    "author": "Eu mesmo"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
